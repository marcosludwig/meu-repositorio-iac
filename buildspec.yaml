version: 0.2
phases:
  install:
    commands:
      - echo "Instalando dependências..."
  build:
    commands:
      - echo "Executando o build..."
artifacts:
  files:
    - infraestrutura.yaml
    
